---
- name: Converge
  hosts: all
  become: true

  pre_tasks:
    - name: Update apt cache.
      apt: update_cache=true cache_valid_time=600
      changed_when: false
      when: ansible_os_family == 'Debian'

  roles:
    - role: jason_riddle.kubeconfig

  # post_tasks:
  #   - name: Verify curl is installed.
  #     command: curl --version
  #     changed_when: false
